#!/usr/bin/env bash

# Usage: timer 10m
#
# Waits for the specified period, then plays a sound effect upon completion.
#
# borrowed from Evan Hahn

set -e
set -u
set -o pipefail

sleep "$1"
sfx ringaling
notify 'timer complete' "$1"
