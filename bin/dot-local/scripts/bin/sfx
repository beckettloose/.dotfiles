#!/usr/bin/env bash
#
# Usage: sfx foo.ogg
#
# Plays a sound effect from a specific directory
#
# borrowed from Evan Hahn

set -e
set -u
set -o pipefail

exec mpv --really-quiet --no-video "$HOME/.local/scripts/resources/sfx/$1.ogg" &
