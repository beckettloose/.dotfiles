#!/usr/bin/env bash
# borrowed from Evan Hahn
set -e
set -u
set -o pipefail

XDG_CONFIG_HOME=${XDG_CONFIG_HOME:-$HOME/.config}
exec mpv --really-quiet --no-video "$XDG_CONFIG_HOME/personal-sfx/$1.ogg" &
