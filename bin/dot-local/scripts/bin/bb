#!/usr/bin/env bash
#
# Usage: bb my_command
#
# Really really run a program in the background.
# You'll never hear from it again. Like `my_command &` but more.
#
# borrowed from Evan Hahn

set -e
set -u

if test -t 1; then
  exec 1>/dev/null
fi

if test -t 2; then
  exec 2>/dev/null
fi

"$@" &
