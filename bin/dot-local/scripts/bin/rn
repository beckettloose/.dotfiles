#!/usr/bin/env bash
#
# Usage: rn
#
# Prints the current time and date using `date` and `cal`.
#
# borrowed from Evan Hahn

set -e
set -u

date "+%l:%M%p on %A, %B %e, %Y"
echo
if hash ncal 2>/dev/null; then
    ncal -b
    exit 0
fi
cal
