#!/usr/bin/env bash
#
# Usage: scratch
#
# Opens a temporary vim buffer. Basically `$EDITOR $(mktemp)`.
#
# borrowed from Evan Hahn

set -e
set -u
set -o pipefail

file="$(mktemp)"
echo "Editing $file"
exec "$EDITOR" "$file"
