#!/usr/bin/env bash
#
# Usage: cpwd
#
# Copies the current directory to the clipboard. Basically just `pwd | copy`.
#
# borrowed from Evan Hahn

set -e
set -u
set -o pipefail

pwd | tr -d '\n' | copy
